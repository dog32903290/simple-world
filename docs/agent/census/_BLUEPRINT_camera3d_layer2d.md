# Build Blueprint: camera3d/Layer2d (Cut 96 scout) — 2 cuts

Verdict: PROCEED, NOT a quagmire. Seam re-frames as a CONTEXT-MATRIX push/pop (Cut 87 context-var precedent), NOT a DX11 camera port (world-view rule). Camera math already written + refuter-SAFE in stash@{0}. Golden closed-form (Option B).

## Seam def (Metal-native)
3 host EvaluationContext matrices CameraToClipSpace / WorldToCamera / ObjectToWorld, pushed before a subtree evals + popped after; at draw, derive ObjectToClipSpace = ObjectToWorld·WorldToCamera·CameraToClipSpace (row-vector order, TransformBufferLayout.cs:13-16) and bind the ONE matrix the quad VS reads. Today (Cut 88) DrawScreenQuad draws RAW CLIP SPACE (vs-draw-viewport-quad.hlsl, mul commented). Layer2d dispatches a DIFFERENT shader Lib:shaders/dx11/draw-Quad-vs.hlsl whose vsMain does `mul(float4(quadVertexInObject,0,1), ObjectToClipSpace)` (draw-Quad-vs.hlsl:54, LIVE). psMain byte-identical to draw_screenquad_fs (clamp(Color*tex,0,(1000,1000,1000,1))).

## Existing infra (reuse, file:line)
- ★stash@{0}: field_camera.h/.cpp = byte-faithful lookAtRH/perspectiveFovRH/inverse/mul (GraphicsMath.cs), refuter-SAFE, row-major/row-vector LOCKED, no-double-transpose (header note), --selftest-field-camera golden, defaultRaymarchTransforms(aspect)=EvaluationContext.SetDefaultCamera (eye=(0,0,2.4142),fov45,near.01,far1000). ★EXTRACT ONLY field_camera.{h,cpp} from stash (git checkout stash@{0} -- <paths>) — do NOT pop the whole stash (it also has PBR-blocked renderField3d/march, leave those).
- Cut 88 executor: point_ops_rendertarget.cpp:103-200 cookRenderTarget + makeDrawPSO Normal/Additive blend :51-92; ScreenQuad branch :185. render_command.h:62-86 RenderDrawItem. draw_params.h DrawScreenQuadParams. draw_screenquad.metal (psMain :48 reuse).
- Tex-into-Command gather: point_graph.cpp:342-404 cookTexNode + FORK#1 srcTexture in Command path (reuse).
- context-var YELLOW precedent (_BLUEPRINT_contextvar_yellow.md): host-side context + push/pop = same architecture for the matrix context.

## CUT 1 (this batch) — camera-context seam + Layer2d at IDENTITY transform (the unlock)
1. EXTRACT field_camera.{h,cpp} from stash@{0} (only those 2 files); add `mat4 objectToClipSpace(objectToWorld, worldToCamera, cameraToClipSpace)` helper (= mat4Mul(mat4Mul(o2w,w2c),c2c)). Verify --selftest-field-camera green.
2. Host transform-context POD (the 3 source matrices + derived ObjectToClipSpace), threaded into the ScreenQuad/Layer2d cook branch via TexCookCtx (mirror context-var YELLOW: function-local context in cookRenderTarget driver, default = defaultRaymarchTransforms(aspect) when no camera op present). F1: function-local in driver, NOT runtime global (keeps runtime→app clean).
3. New VS `draw_quad_xf_vs` in a .metal (PS reuses draw_screenquad_fs). ONLY diff vs draw_screenquad vsMain = `M * float4(quadVertexInObject,0,1)` (M=ObjectToClipSpace). Build ObjectToClipSpace into a 16-float field in DrawScreenQuadParams (or a new params struct).
4. New `DrawKind::Layer2d` (F2: parallel to ScreenQuad, NOT a useTransform flag — keeps clip-space ScreenQuad leaf untouched; TiXL uses 2 distinct shaders) + ObjectToClipSpace in the params.
5. Proving op: **Layer2d** at Scale=1/Stretch=1/Rotate=0/ScaleMode=Stretch, default camera → ObjectToWorld=Identity → ObjectToClipSpace = default WorldToCamera·CameraToClipSpace. Backward-trace Layer2d.t3 → it dispatches draw-Quad-vs.hlsl; port vsMain (the mul) + reuse psMain. (Layer2d.t3 internal: _ProcessLayer2d→ApplyTransformMatrix→TransformsConstBuffer→draw-Quad-vs; for Cut 1 identity, skip the SRT math = ObjectToWorld stays Identity.)
6. ★F3 anti-over-build: build ONLY ObjectToClipSpace (the 1 matrix the VS reads). The other 9 TransformBufferLayout matrices are DEAD for Layer2d — do NOT port them.

### GOLDEN (Option B closed-form, non-tautological):
- math tooth (Option A, reuse stash --selftest-field-camera) — pins the projection PARAMETERS: add 1 host assert — default camera + ObjectToWorld=Identity → ObjectToClipSpace; transform world origin (0,0,0,1)·M /w → NDC (0,0) dead center (origin on camera axis). A wrong multiply-order/handedness moves that center (this is where multiply-order/handedness/fov/matrix-entry errors are caught — analytically, not on the render leg).
- render tooth (Option B) — pins SEAM-PRESENCE (structure), NOT parameters: render a SOLID-color source texture (uniform red, via existing RenderTarget known-fill, runRenderTargetSelfTest precedent point_ops_rendertarget.cpp:194) through Layer2d at default camera, Width/Height small so the quad projects to a known center band. Readback CENTER pixel = clamp(Color*texColor,0,1000) (PS arithmetic) AND a far-corner pixel = background/clear. ★injectBug = use the raw-clip VS (drop the mul) → quad fills a different/larger region → a known off-center "should-be-background" pixel reads quad-color → RED. This proves the camera matrix THREADS host→VS and the mul is LOAD-BEARING (fails iff a dropped/collapsed transform — drop-mul, x-axis collapse). It does NOT verify the projection PARAMETERS: a wrong-but-plausible projection (e.g. wrong fov 60° vs 45°, or a two-translation multiply-order swap) can PASS this render leg — those are the math tooth's job. The two teeth are complementary: render = structure, math = parameters. (Cut-2 NOTE — abandoned idea: an edge-probe "fov tooth" on the render leg was proposed and then BUILT in Cut 2, but an adversarial refuter proved it HOLLOW and it was REMOVED. At the default camera the z=0 visible half-extent is d·tan(fov/2)=1.0 for EVERY fov, so a z=0 world point maps to the SAME NDC regardless of fov — the render leg structurally CANNOT see fov. The built version's (a) leg rendered through the only camera the executor has (45°), so it was just an edge-coverage check already covered by the scale tooth; its (b) "wrong 60° fov" leg was pure host-side perspectiveFovRH(60°) vs (45°) math never routed through the shipped render path. fov / matrix-entries / multiply-order stay the math tooth's job — that is by design, NOT a gap on the render leg.)
- DISCIPLINE: assert ONLY deep quad interior (center plateau) + far background corner. NEVER a quad edge (perspective+raster sub-pixel). single-sample (no MSAA), no depth (Layer2d EnableDepthTest=false default — pin it).
- Compute expected NDC host-side via the SAME mat4TransformPointDivW the golden trusts (don't hardcode the rect). Center pixel is perspective-invariant (origin→NDC center always).

### Files Cut 1 (~5-6): field_camera.{h,cpp}(extract from stash), draw_quad_xf.metal, point_ops_rendertarget.cpp, draw_params.h, render_command.h, point_ops_layer2d.cpp + golden, CMake/selftest wiring.

## CUT 2 (next) — Camera op (explicit camera push/pop) + transform-stack
- Camera op (Camera.cs:38-44) pushes WorldToCamera/CameraToClipSpace around subtree (YELLOW writer-before-reader ordering); reuse stash raymarchTransforms(eye,target,up,fov). Drop offset/roll/lensShift v1 (commented embellishments Camera.cs:82-100, named fork). Golden: eye=(0,0,5) farther→quad projects smaller→a mid-radius pixel flips quad→background (perspective scale = distance ratio).
- Transform op (render/_/Transform) + _ProcessLayer2d ScaleMode(FitHeight/Width/Both/Cover/Stretch _ProcessLayer2d.cs:55-105)/Rotate/Stretch → ObjectToWorld composition. Golden: Scale=2 quad grows.
DEFER beyond cut2: OrbitCamera/ActionCamera (temporal/input R3), gizmos (Phase C after seam), depth-test/depth-buffer (GodRays/MotionBlur diff seam), DrawScreenQuadAdvanced, full 10-matrix layout.

## Forks: forced=row-major/no-double-transpose, multiply order, default camera params, psMain clamp. choice=F1 driver-local context / F2 new DrawKind::Layer2d / F3 only-ObjectToClipSpace. None for 柏為 (output mechanically determined by TiXL math).
## Risks: (1) perspective pixel placement → assert center+corner only, compute expected via stash mat4TransformPointDivW. (2) NDC z [0,1] D3D==Metal aligned; viewport Y-flip already handled by texCoord*(0.5,-0.5)+0.5 + Cut88 proved clip→pixel for no-transform path → only the mul is new. (3) ScaleMode aspect math = Cut 2 (Cut 1 ScaleMode=Stretch simplest). (4) no depth v1 (pin). MSAA not a risk (single-sample).
## Critical files: external/tixl Operators/Lib/render/Layer2d.{cs,t3} + Camera.cs + _ProcessLayer2d.cs + ApplyTransformMatrix.cs + TransformsConstBuffer.cs + TransformBufferLayout.cs + Assets/shaders/dx11/draw-Quad-vs.hlsl; sw field_camera.{h,cpp}(stash), point_ops_rendertarget.cpp, render_command.h, draw_params.h, draw_screenquad.metal, point_ops_drawscreenquad.cpp(template).
